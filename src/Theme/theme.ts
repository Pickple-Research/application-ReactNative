import { themeColors } from "./color.theme";
import { themeSizes } from "./size.theme";

/**
 * 모든 테마를 가져와 배포합니다.
 * @author 현웅
 */
export const theme = {
  color: themeColors(),
  size: themeSizes,
};
