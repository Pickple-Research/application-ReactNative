import { color, size } from "./Value";
import { buttonTheme, textTheme, viewTheme } from "./Component";

/**
 * 모든 테마를 가져와 배포합니다.
 * @author 현웅
 */
export const theme = {
  color,
  size,
  button: buttonTheme,
  text: textTheme,
  view: viewTheme,
};
